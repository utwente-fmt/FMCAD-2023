# configurations

SIM_END = 100 # FIXME change name (how many times you schedule without finding new states)
SIM_COUNT = 100 # FIXME change name (how many times you run the simulation from the start)

# config (don't change)

TIME_EV  = 0
DELTA_DELAY = -1

PC_SCHED_VAR = "pc"
INIT_SCHED_VAR = "init"
READY_SCHED_VAR = "ready"
WAITON_SCHED_VAR = "wait_on"
TIMEOUT_SCHED_VAR = "ev_timeout"
DELTA_SCHED_VAR = "delta_delay"
OCCURRED_SCHED_VAR = "occurred"